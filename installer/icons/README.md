# Icon Placeholders

Add final installer/application icons here:

- `mac/icon.icns`
- `win/icon.ico`
- `linux/` (PNG set, include `512x512.png`)

You can also keep editable source files here (for example, `source/icon-master.png`).
